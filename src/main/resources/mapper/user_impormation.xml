<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.example.nalot.dao.UserDao">
    <select id="selectUserList" resultType="com.example.nalot.model.UserDto">
        select * from user_information
    </select>

    <select id="selectUserInfo" resultType="com.example.nalot.model.UserDto">
        select * from user_information
        where id = #{userId}
    </select>

    <insert id="insertUserInfo" parameterType="com.example.nalot.model.UserDto">
        insert into user_information
            (id, password, name, gender, zone_code, address_basic, address_detail)
        values (#{id}, #{password}, #{name}, #{gender},
                #{zoneCode}, #{addressBasic}, #{addressDetail})
    </insert>

    <update id="updateUserInfo" parameterType="com.example.nalot.model.UserDto">
        update user_information
        set password=#{password}, name=#{name},
            gender=#{gender}, zone_code=#{zoneCode},
            address_basic=#{addressBasic}, address_detail=#{addressDetail}
        where id=#{id};
    </update>

    <delete id="deleteUserInfo" parameterType="com.example.nalot.model.UserDto">
        delete user_information where id=#{id};
    </delete>
</mapper>